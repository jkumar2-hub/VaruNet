version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
      - "80:80"
    environment:
      - PYTHONPATH=/app/backend
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
